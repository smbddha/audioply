import React from "react";

import { INode } from "@/types";

type Props = {
  node: INode<AudioDestinationNode>;
};

const AudioOutNode = (props: Props) => {
  const { node } = props;

  return <></>;
};

export default AudioOutNode;
